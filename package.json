{
  "name": "board-app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "format": "rome format .",
    "format:fix": "rome format --write .",
    "lint": "rome check .",
    "lint:fix": "rome check --apply .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "rome": "^11.0.0"
  }
}
